# CLAUDE.md

Rust로 Redis를 바닥부터 다시 만드는 **학습용** 프로젝트입니다. 3인 스터디로 진행하며,
참여자들은 Rust를 거의 모르는 상태에서 AI와 함께 구현합니다.

**이 저장소에서 가장 중요한 규칙: 빨리 완성하는 것보다, 참여자가 코드를 이해하는 것이 우선입니다.**
동작하는 코드를 한 번에 쏟아내면 이 프로젝트는 실패합니다.

## 작업 규칙

### 한 번에 하나씩
- 요청받은 커맨드/기능 **하나만** 구현합니다. 시키지 않은 커맨드를 미리 만들지 않습니다.
- "Redis를 만들어줘" 같은 광범위한 요청을 받으면, 먼저 어느 단계인지 물어봅니다.
- 리팩터링과 기능 구현을 한 커밋에 섞지 않습니다.

### 설계 선택지가 둘 이상이면 멈추고 물어봅니다
이 스터디의 핵심은 설계 토론입니다. 다음과 같은 갈림길에서는 **바로 구현하지 말고**,
선택지와 트레이드오프를 정리해서 사람에게 물어봅니다.

- 동시성 모델 (`Arc<Mutex<HashMap>>` / 샤딩 / actor + 채널)
- 에러 처리 방식 (직접 만든 `enum` / `anyhow` / `Box<dyn Error>`)
- 새로운 의존성 추가
- 자료구조 선택 (`HashMap` vs `BTreeMap`, `VecDeque` vs `Vec` 등)
- 블로킹 동작 구현 방식

결정이 내려지면 그 근거를 PR 본문에 적습니다.

### 금지
- **Redis 프로토콜/파서 crate 사용 금지.** RESP 파싱은 직접 구현합니다.
  (`redis` crate는 테스트 코드에서만 허용)
- `unsafe` 금지. 필요하다고 판단되면 먼저 사람에게 물어봅니다.
- 프로덕션 코드에서 `unwrap()` / `expect()` 금지. 잘못된 입력 하나로 서버 전체가 죽으면 안 됩니다.
  (테스트 코드에서는 허용)
- 컴파일 에러를 `.clone()`으로 덮지 않습니다. clone이 정말 필요하면 왜 필요한지 설명합니다.
- 요청받지 않은 파일 정리, 포맷팅, 리팩터링 금지.

### 설명 의무
작업이 끝나면 다음 두 가지를 반드시 함께 남깁니다.

1. **이번에 새로 등장한 Rust 개념**을 3줄 이내로 설명 (예: `Arc`가 왜 필요한지)
2. 사람이 "이 줄 왜 있음?"이라고 물으면 답할 수 있도록, 비자명한 코드에만 주석

## 서버 규약

`scripts/difftest.sh`가 우리 서버를 이렇게 실행합니다. 이 인터페이스는 바꾸지 않습니다.

```
cargo run -- --port <포트번호>
```

- 기본 포트는 6380입니다. (진짜 Redis가 6379를 쓰기 때문)
- 응답은 **진짜 Redis와 바이트 단위로 동일해야** 합니다. 에러 메시지 문구까지 똑같이 맞춥니다.

## 검증

작업 완료를 선언하기 전에 아래를 모두 실행하고, 결과를 확인합니다.
실행하지 않고 "통과할 것입니다"라고 말하지 않습니다.

```bash
cargo fmt
cargo clippy -- -D warnings
cargo test
./scripts/difftest.sh          # 진짜 Redis와 응답 비교
```

## 커밋 / PR

- 커밋 메시지는 한국어로, 작고 논리적인 단위로 나눕니다.
- 브랜치 이름: `<이름>/s03-set-get` 형식 (세션 번호 포함)
- `main`에 직접 커밋하지 않습니다. 반드시 PR을 거칩니다.
- PR 본문은 `.github/PULL_REQUEST_TEMPLATE.md`를 채웁니다. 설계 선택 항목은 비워두지 않습니다.

## 디렉터리

```
src/                 서버 구현
scripts/difftest.sh  진짜 Redis와 응답을 비교하는 테스트 러너
tests/cases/         세션별 비교 테스트 케이스 (한 줄에 커맨드 하나)
```

## 참고

- RESP 프로토콜 스펙: https://redis.io/docs/latest/develop/reference/protocol-spec/
- 커맨드 레퍼런스: https://redis.io/docs/latest/commands/
- tokio `mini-redis`는 **해당 세션을 끝낸 뒤에만** 참고합니다. 미리 보면 학습 효과가 사라집니다.
